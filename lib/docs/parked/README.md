# Parked

Client-facing templates, kept and not wired.

`buildDocuments` produces one document now, for Dan. These two come back when
the correction ledger has taught the tool something worth putting in front of a
client — at which point the proposal should also stop hardcoding a return site
visit and a "being quoted" access line, both of which belong to one job in
Pittsford and not to masonry in general.

Nothing imports these. They will not compile against the current spec shape
without work; that is expected and is not a regression.
